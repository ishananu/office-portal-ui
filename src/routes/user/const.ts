interface IDashabordRoutes {
  dashboard: string;
  employees: string;
  library: string;
  products: string;
}
const routes: IDashabordRoutes = {
  dashboard: '/dashboard',
  employees: '/employees',
  library: '/library',
  products: '/products'
};

export default routes;
